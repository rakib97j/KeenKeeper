import React from 'react';

export const metadata = {
  title: "TimeLine",
  description: "This is  Timeline page",
};

const TimeLinePage = () => {
    return (
        <div>
            this is timeline page
        </div>
    );
};

export default TimeLinePage;