import React from 'react';

export const metadata = {
  title: "Stats",
  description: "This is Stats Page",
};

const StatsPage = () => {
    return (
        <div>
            this is state page
        </div>
    );
};

export default StatsPage;